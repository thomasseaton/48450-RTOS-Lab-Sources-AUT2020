What's the Makefile for?
========================

It's a shortcut to use GCC to compile individual C files, use it like so:

	$ make P1-fork-if-else

to compile P1-fork-if-else.c

You can also run 

	$ make all

To compile all of the sources.
